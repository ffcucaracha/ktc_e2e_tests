[pytest]
addopts = -ra
testpaths = tests
markers =
    data_collection: creates persistent training sessions for ML dataset collection
    presentation: captures curated screenshots for the championship presentation
    video_demo: runs a paced visible-browser walkthrough intended for screen recording
